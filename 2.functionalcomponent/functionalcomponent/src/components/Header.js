/**
 * Header component that displays a header with a title.
 * @returns {JSX.Element} JSX representation of the header component.
 */
import React from 'react';

function Header() {
    return (
        <div>
            <h1>This is the header component</h1>
        </div>
    );
}

export default Header;
